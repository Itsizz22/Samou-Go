# تقرير حالة سموع كويك — 9 سبتمبر 2026

> تحديث بعد تنفيذ تحسينات التشغيل: التعديلات التالية موجودة محليًا ولم تُنشر بعد، وتحتاج تطبيق migration المرفقة عند النشر. الفقرات اللاحقة توثق نقطة الفحص السابقة ولا تعني أن الميزات الجديدة نُشرت على Vercel.
>
> - اختيار حتى 12 منتجًا مميزًا وترتيبها من إعدادات الإدارة، مع اشتراط صورة ومنتج متاح من متجر معتمد. القسم يعتمد الآن هذه الاختيارات، ويُخفى إن لم توجد اختيارات صالحة.
> - حماية الطلب الواحد والسلة متعددة المتاجر بمعرّف محاولة؛ تُحفظ النتيجة والطلبات في معاملة قاعدة بيانات واحدة. إعادة الطلب نفسه تعيد النتيجة ولا تكرر التنبيهات. تغيير بيانات محاولة نتيجتها غير مؤكدة يتطلب التحقق أولًا؛ لا توجد إعادة إرسال خفية لسلة مختلفة.
> - لوحة إدارية للطلبات المعلقة أكثر من 5 دقائق، تعرض الأقدم أولًا مع اتصال وواتساب، وتتحدث كل 15 ثانية.
> - منع الإضافة من صفحة متجر متوقف عن استقبال الطلبات، وفرض الحالة أيضًا على الخادم.
> - رسائل انقطاع الاتصال وتأخر الخادم مع الاحتفاظ بالسلة، وتصحيح وصف بنر التتبع.
> - بقي المحتوى التجريبي محفوظًا؛ إخفاؤه أو استبداله قبل الجمهور قرار تجهيز بيانات، وليس حذفًا تلقائيًا.
>
> التحقق: 275 اختبار API ناجحًا، و32 فحص API محليًا تتضمن محاولتين متزامنتين ومعرّف الطلب نفسه، وإعادة سلة متعددة المتاجر، وصلاحيات اختيار المنتجات وترتيبها. نجح فحص المتصفح للإدارة والاختيارات ورابط المنتج وحالة انقطاع الشبكة. أخطاء الشبكة التي ظهرت عند قطع الإنترنت عمدًا متوقعة، ولا تُعد استثناءات JavaScript. أُلغيت طلبات الاختبار واستُعيد اختيار المنتجات الإداري السابق بعد الاختبار.


## التقييم العام

نسخة الويب مناسبة لإطلاق تجريبي محدود مع متابعة بشرية للطلبات. لا أعتبر الاختبارات الحالية وحدها اعتمادًا لإطلاق عام غير مراقب أو للنشر في App Store وGoogle Play. يجب فصل نجاح البناء وصفحات الويب عن ضمان الإشعارات والأداء على الأجهزة الحقيقية.

آخر نسخة منشورة ومتحقق منها: `54aae8c`؛ نجحت CI وعملية نشر المواقع السبعة على Vercel. إضافة رابط المنتج الموضحة أدناه موجودة الآن في ملفات العمل المحلية، ولم تُنشر ضمن ذلك الإصدار.

## الإضافة المنجزة اليوم

- الضغط على بطاقة المنتج المميز يفتح متجره مع `productId` في الرابط.
- صفحة المتجر تختار الفئة التي تحتوي على المنتج، ثم تمرر الصفحة إلى بطاقته وتنقل تركيز لوحة المفاتيح إليها.
- زر إضافة المنتج/تخصيصه مستقل عن رابط البطاقة.
- التقاط مؤشر السحب يبدأ بعد حركة فعلية، حتى لا يمنع الضغط الطبيعي على الرابط.
- احترام إعداد تقليل الحركة عند الانتقال للمنتج.

الملفات المعدلة: `FeaturedProductsShowcase.tsx` و`useShowcaseCarousel.ts` و`StoreDetailScreen.tsx` في تطبيق العميل.

### فحص الإضافة

- نجح typecheck وبناء تطبيق العميل.
- نجح الضغط على المنتج المميز الحقيقي والوصول إلى بطاقته، دون أخطاء Console.
- نجح رابط مباشر لبيتزا المتجر التجريبي واختيار فئة البيتزا بدل الفئة الأولى.
- نجح اختبار سحب ببيانات استجابة اختبارية من منتجين: تحرك العرض ولم يفتح رابطًا بالخطأ. لم يغير هذا الاختبار قاعدة البيانات.
- نجح استقلال زر الإضافة عن رابط البطاقة.
- الصور المرفقة من نسخة المعاينة المحلية المتصلة بالبيانات الحالية على الخادم، وليست تصاميم مولدة.

## نتائج الفحص السابق على Vercel

| الجزء | ما تم التحقق منه |
|---|---|
| العميل | الرئيسية، البحث، المتجر، السلة، بوابة المصادقة، الحساب، الطلبات والتتبع، الإعدادات، العروض، المفضلة، الدعم والطلبات المخصصة |
| صفحات مستقلة | تفاصيل المتجر، إتمام الطلب، تتبع الطلب، وروابط التنقل بينها |
| مدير المتجر | لوحة الرئيسية، الطلبات، المنتجات، العروض، إعدادات المتجر، الطلبات المخصصة وكشف الحساب |
| الكابتن | الطلبات، واجهة الخريطة/البديل، الأرباح، الحساب والرئيسية |
| الإدارة | المتاجر، السائقون، العملاء، الطلبات، المناطق، العروض، الإعدادات والمالية |

الفحص شمل 49 خطوة ضمن 8 رحلات (العميل المسجل والضيف محسوبان منفصلين) على المواقع السبعة: لا أخطاء أو تحذيرات Console، لا استثناءات JavaScript، لا فشل طلبات HTTP، ولا تجاوز عرض في اختبار 375px. هذه نتيجة المسارات والحالات المفحوصة، وليست إثباتًا لاستحالة ظهور خطأ في حالة أخرى.

تمت تجربة خيارات البيتزا والسعر المحسوب من الخادم، وإنشاء طلب تجريبي على الإنتاج وقبوله ثم إلغائه. جرى فحص رفع الصور عبر مسار الرفع الفعلي. اختبارات API السابقة: 271 اختبارًا؛ واختبارات api-client ذات الصلة بالصوت والتنقل: 25 اختبارًا.

## إصلاحات موجودة في الإصدار المنشور

1. تأخير تهيئة الصوت إلى حين تفاعل المستخدم، ومعالجة انتهاء مؤقت التنبيه عند توقف ساعة الصوت.
2. تصحيح روابط التنقل بين المواقع المستقلة وروابط الحساب والسلة.
3. تخزين الملفات المعالجة في Neon قبل اعتماد الرفع؛ ملفات Render المحلية أصبحت نسخة مؤقتة يمكن استعادتها.
4. اختبار استعادة صورة مطابقة بايتًا ببايت من Neon دون وجود الملف محليًا؛ نجح الفحص أيضًا بعد النشر.
5. تثبيت إصدار Vercel CLI المستخدم بالنشر، بعد فشل إصدار `latest` أثناء عملية نشر سابقة.

## نقاط محتوى وتجهيز ظاهرة حاليًا

- مصدر المنتجات المميزة يعيد منتجًا واحدًا حاليًا: شاورما دجاج. لهذا لن يحدث تبديل تلقائي بين منتجات متعددة الآن. الكود يدعم التبديل كل 3.5 ثانية عندما توجد منتجات متعددة، بالترتيب وليس عشوائيًا.
- المنتج المذكور بلا صورة في بياناته، فيظهر بديل بالأحرف. هذا مختلف عن صورة منتج قديمة مفقودة من التخزين السابق.
- 11 صورة تجريبية مستعادة تعمل، وصورة قديمة أخرى مفقودة تحتاج إعادة رفع أصلها؛ لا يمكن استعادة ملف غير موجود من مجرد رابط.
- توجد متاجر ومنتجات وحسابات تجريبية. قبل فتح الاستخدام العام يجب تجهيز الكتالوج الحقيقي وإخفاء/أرشفة المحتوى التجريبي بخطة واضحة، دون حذف طلبات أو بيانات عمل حقيقية.
- تتبع GPS المباشر معطل عمدًا بالعلم `ENABLE_LIVE_GPS_TRACKING: false`. التشغيل الحالي يعتمد على الحالة والمنطقة والوصف. يجب أن تبقى الرسائل التسويقية متوافقة مع ذلك.

## ما ينقص قبل الاستخدام العام

### أولوية عالية

1. **نشر إضافة رابط المنتج** ثم إعادة اختبارها على Vercel، وتضمينها في APK جديد إذا كانت النسخة المحمولة تستخدم ملفات ويب مدمجة.
2. **اعتمادية الخادم:** تأخر Render عند الاستيقاظ قد يؤخر دخول المستخدم أو الطلبات. تحقق من إعداد خدمة دائمة التشغيل قبل الاعتماد التجاري. لم يتم تغيير خطة الاستضافة أو اعتماد أداء وقت الذروة في هذه الجولة.
3. **دورة عمل فعلية:** تنفيذ طلبات مراقبة بين عميل ومتجر وكابتن حقيقيين حتى التسليم، مع الإلغاء ورفض الطلب وفقدان الشبكة ومحاولة الضغط المكرر. قبول طلب تجريبي ثم إلغاؤه لا يثبت كل هذه الحالات.
4. **الإشعارات:** إعادة اختبار آخر نسخة موقعة على أجهزة Android حقيقية في المقدمة والخلفية وبعد الإغلاق، مع رفض الإذن وتوفير البطارية وتبديل الحساب. نجاح سابق أو تصريح المستخدم بأن إشعارات الإغلاق تعمل لا يعوض فحص النسخة النهائية.
5. **البيانات والحسابات:** اعتماد حسابات التشغيل وأرقام التواصل والمناطق والصور والأسعار وأوقات المتاجر، وتغيير/تعطيل بيانات الدخول التجريبية قبل الجمهور.

### Android

ملف Gradle الذي روجع يحمل `versionCode 1` و`versionName 1.0`، ولا يحتوي بنفسه على `signingConfig` للإصدار النهائي. لا يعني هذا بالضرورة عدم وجود توقيع خارجي، لكنه لم يُتحقق منه هنا. يلزم التحقق من مفتاح التوقيع الدائم ونسخه الاحتياطي، ورفع رقم الإصدار، وبناء APK/AAB نهائي، ثم اختباره على جهاز فعلي. لم يتم بناء APK جديد في هذه الجولة.

### iPhone

ملف `themes/web-customer/ios/README.md` يذكر بوضوح أن البناء الأصلي وتشغيل Xcode وتسليم APNs/FCM لم يتم التحقق منها على هذا الجهاز Windows. يلزم Mac/Xcode، ملف Firebase iOS الحقيقي، مفتاح APNs، Apple Developer والتوقيع والصلاحيات، ثم TestFlight وتجربة iPhone حقيقي. واجهة تنبيه Android الكاملة لا تعني أن iOS يسمح بالسلوك نفسه عند إغلاق التطبيق.

### التشغيل والمراقبة

- التحقق من النسخ الاحتياطي لقاعدة Neon وتجربة الاستعادة، ومراقبة حجم الملفات المخزنة وتكلفتها. التخزين الدائم يحل ضياع الملفات لكنه لا يثبت وجود خطة نسخ احتياطي معتمدة.
- مراقبة أخطاء الخادم ووقت الاستجابة وفشل الإشعارات والطلبات، مع تنبيه للمسؤول عند الانقطاع.
- اختبار تحميل متزامن واقعي قبل حملة تسويق كبيرة؛ لم يجر اختبار ضغط في هذه الجولة.
- اعتماد سياسة الخصوصية وشروط الاستخدام والدعم وإجراءات الحسابات بما يتوافق مع التشغيل الفعلي ومتطلبات المتاجر.
- عدم إعلان الدفع الإلكتروني كميزة جاهزة قبل فحص بوابته ودورة نجاح/فشل/استرداد فعلية؛ مسار الطلب الذي فُحص لا يعتمد إثبات جاهزية دفع إلكتروني.

## قرار الإطلاق المقترح

ابدأ تشغيلًا تجريبيًا محدودًا بعد نشر الإضافة وتجهيز الحسابات والكتالوج والتحقق من إشعارات الأجهزة، مع مسؤول يتابع كل طلب. الانتقال للإطلاق العام يكون بعد إغلاق البنود عالية الأولوية وتوثيق دورة تسليم كاملة. لا يوجد أساس مهني لوعد «صفر أخطاء دائمًا»، لكن الاختبارات الحالية توفر نقطة بداية واضحة وقابلة للمتابعة.
## تحديث مقدمة التشغيل

تم تضمين فيديو الانترو (6 ثوانٍ، 1080×1920، 60fps، نحو 516KB) وصورة بديلة داخل أصول تطبيق العميل، ونسخهما إلى مشروع Android باستخدام Capacitor sync. يظهر الفيديو عند بدء تحميل التطبيق فقط، ثم ينتقل المستخدم الجديد لاختيار المنطقة، والضيف السابق للرئيسية، وصاحب الجلسة المحفوظة للرئيسية دون إعادة شاشة البداية. تُحفظ الروابط المباشرة لصفحات الطلبات. عند تعذر تشغيل الفيديو توجد صورة بديلة وحد انتظار يمنع تعطيل الدخول، وتقليل الحركة يعرض الصورة الثابتة.

التحقق: تشغيل الفيديو الفعلي، أول استخدام، الضيف العائد، جلسة حساب محفوظة دون علامة onboarding، عدم إعادة التشغيل أثناء التنقل، إعادة التشغيل عند إعادة تحميل التطبيق، تعذر تحميل الفيديو، وتقليل الحركة. نجح فحص الأنواع والبناء، ولا استثناءات JavaScript في الفحص. لم يتم بناء أو تثبيت APK جديد أو نشر تحديث Vercel في هذه الجولة.
## فحص الجلسة والمناطق قبل الرفع

أضيف تجديد استباقي للتوكن المنتهي قبل الطلبات المحمية، مع مشاركة عملية التجديد بين الطلبات المتزامنة. تبقى المناطق عامة ولا ترسل بيانات الجلسة. تحميل المناطق يعيد المحاولة مرتين للأعطال المؤقتة، ويلغي العمل عند إزالة المكوّن ويعيد التحميل عند عودة الاتصال. لا يعني ذلك أن كل 401 يمكن منعه: الجلسة الملغاة فعلياً تتطلب تسجيل الدخول.

نجحت 30 حالة اختبار لعميل API و275 حالة للخادم، وبناء العميل. نجح اختبار المتصفح لاستعادة المناطق بعد 503 ولإعادة التحميل عند الاتصال، دون استثناءات JavaScript. هذا الفحص محلي؛ نتيجة نشر الإنتاج تُراجع منفصلة بعد push.
