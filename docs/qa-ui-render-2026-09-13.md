# فحص الواجهة والأداء — 13 سبتمبر 2026

## Render الفعلي
- الهدف: https://samou-go.onrender.com/api/v1
- k6 v2.2.0، اختبار قراءة فقط: المتاجر، المناطق، المنتجات المميزة، منتجات متجر.
- تصاعد حتى 25 مستخدمًا افتراضيًا، مدة إجمالية قرابة 160 ثانية، منها 60 ثانية عند الحمل الأعلى.
- 1,425 طلب HTTP؛ صفر أخطاء. نجحت 1,424 عملية تحقق (الطلب الإضافي للإعداد).
- المتوسط 90.82ms، p95=118.47ms، p99=150.13ms، الأقصى 463.37ms.
- المعدل العام 8.89 طلب/ثانية، ويشمل مراحل التصاعد والانخفاض وفترات الانتظار.
- لم يتم الوصول إلى حد انهيار الخدمة. هذه نتيجة تحمل التصفح عند الحمل المقاس، وليست سعة قصوى أو قياسًا لإنشاء الطلبات/الدفع/رفع الصور/الإشعارات.
- الحماية المعلنة من الخادم: 1200 طلب/دقيقة لكل IP؛ لم تُعطّل ولم تُتجاوز. قياس سعة أعلى يحتاج مولدات حمل موزعة مصرح بها ومراقبة موارد Render وقاعدة البيانات.
- لم تُغيّر بيانات الإنتاج ولم تُرسل رسائل أو إشعارات.

## المحلي والواجهة
- بناء web-customer نجح.
- فحص تخصيص المنتجات والمحرر والتواصل مع الكابتن نجح عند 320/390/768px ببيانات محاكاة.
- بطاقة السلة: توزيع الصورة والتفاصيل وأزرار التحكم، أسماء من سطرين، أهداف لمس 44px. فحص 320/390px: الكمية والملاحظات والحذف وعدم تجاوز عرض الشاشة؛ مراجعة صورة الناتج.
- k6 محلي SQLite معزول: 15 مستخدمًا، 1653 طلبًا، صفر أخطاء، p95=16.04ms. لا تُقارن هذه الأرقام مباشرة بإنتاج PostgreSQL.
- أُغلقت أدوات المتصفح وخادم Node المحلي وحُذفت بيانات الفحص المحلية.
- تعديلات الواجهة محلية؛ لم يتم النشر أو بناء APK في هذه الجولة.

## دورة الطلب وتحسين التحديث في الخلفية
- اختبار كتابة k6 محصور برمجيًا في localhost: `scripts/load-tests/local-order-cycle.js`.
- دورة فعلية عبر API: تسعير، إنشاء، تفاصيل المتجر، قبول، تحضير، جاهزية، استلام الكابتن برمز المتجر، جلب PIN كزبون، ثم التسليم بالرمز.
- 77 دورة ناجحة / 77، و693 عملية HTTP، صفر أخطاء، حتى 5 VUs، مدة نحو 56 ثانية.
- متوسط HTTP 38.76ms؛ p95=91.63ms؛ أقصى 293.72ms. هذا اختبار SQLite محلي فقط وليس PostgreSQL/Render.
- عينة موارد العملية المحلية أثناء التشغيل: RSS نحو159MiB. عدادات Prisma محلية تراكمية تشمل الإعداد والفحص التمهيدي؛ لا تمثل زمن دورة واحدة ولا موارد Render.
- تحسين مشترك: إيقاف polling في useApi عندما document.hidden، واسترجاع البيانات فور العودة؛ لا يغير الإشعارات أو التتبع.
- 31 اختبار api-client نجح، منها اختبارا الخلفية/العودة والتنظيف؛ typecheck نجح.
- 12 اختبار push/assignment نجح بمحاكاة المزود. Firebase معطل في الاختبار؛ لم يُختبر وصول تنبيه فعلي إلى هاتف.
- لا دليل من هذا الحمل يستدعي إضافة فهارس أو كاش أو طابور؛ الإشعارات الموجودة بالفعل لا تنتظرها استجابة إنشاء الطلب.
- تعذر اتصال أداة المتصفح بلوحة Render (فشل تهيئة kernel assets). لم تتوفر مراقبة CPU/RAM/DB الإنتاجية أو بيئة PostgreSQL مماثلة للإنتاج، لذلك لا يُدّعى اكتمال قياس السعة القصوى.
- أُغلق خادم الفحص وحُذفت طلباته وحساباته المحلية. التعديل لم يُنشر.
