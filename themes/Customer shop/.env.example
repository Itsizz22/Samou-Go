# Base URL of the Samou' Go Express API — version prefix included, no trailing slash.
# Copy to `.env.local` (gitignored) and adjust per environment.
#
# The app falls back to this exact value when the variable is unset, so local
# development works with no .env file at all.
VITE_API_URL=http://localhost:4000/api/v1
